server:
  port: 1001

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:1111/eureka}
  instance:
    prefer-ip-address: false
    secure-port-enabled: true
    non-secure-port-enabled: false
