spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/master

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka